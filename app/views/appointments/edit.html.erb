<h2 class="form_title">Edit Appointment Details</h2>

<%= render 'form' %>

<%= button_to 'Delete Appointment', patient_appointment_path(@patient, @appointment), method: :delete, data: { confirm: 'Are you sure?' } %>
