<%= form_for [@patient, @appointment] do |f| %>
    <%= f.label :date, 'Date' %>
    <%= f.text_field :date %>

    <%= f.label :time, 'Time' %>
    <%= f.text_area :time %>

    <%= f.label :provider_name, 'Provider' %>
    <%= f.text_area :provider_name %>

    <%= f.submit %>
 <% end %>
