<div class ="form_content"><h2 class="edit_patient">Edit Patient Information</h2></div>

<%= render 'form' %>

<div class="form_content"><%= button_to 'Delete Patient', patient_path(@patient), method: :delete, data: { confirm: 'Are you sure?' } %>
<%= link_to "List of Patients", patients_path %></div>
